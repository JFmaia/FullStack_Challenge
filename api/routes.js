import Router from '@koa/router' //Rotas da api

export const router = new Router();

router.get('/tweets', ctx => {
    
});

router.post('/tweets', ctx => {
   
});

/*Info

    Body conteudo da request.
    Query é o que vem na url da request.

*/ 